import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export const metadata = { title: "Für Vereine" };

export default function FuerVereinePage() {
  return <PlaceholderPage title="Für Vereine" />;
}
